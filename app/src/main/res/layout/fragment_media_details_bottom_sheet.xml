<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bottom_sheet_background">

    <!-- Top section with Close Button and Title -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- FIX: Close Button moved to top-left and styled -->
        <ImageButton
            android:id="@+id/close_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="top|end"
            android:background="@drawable/close_button_background"
            android:contentDescription="@string/close_button_description"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@android:color/white" />

        <TextView
            android:id="@+id/detail_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:text="@string/media_title_placeholder"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

    </FrameLayout>

    <TextView
        android:id="@+id/detail_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/media_description_placeholder"
        android:textColor="#E0E0E0"
        android:visibility="gone"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/detail_file_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:lineSpacingExtra="4dp"
        android:text="@string/file_info_placeholder"
        android:textColor="#B0B0B0"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/available_storage_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/available_storage_placeholder"
        android:textColor="#00E676"
        android:textSize="12sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/applied_settings_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/applied_settings_placeholder"
        android:textColor="#00E676"
        android:textSize="12sp"
        android:textStyle="bold"/>

    <!-- DOWNLOAD PROGRESS AREA -->
    <LinearLayout
        android:id="@+id/download_progress_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp"
        android:visibility="visible">

        <TextView
            android:id="@+id/download_status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/download_status_placeholder"
            android:textColor="#A9A9A9"
            android:textSize="12sp" />

        <ProgressBar
            android:id="@+id/download_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:max="100" />
    </LinearLayout>

    <!-- Horizontal LinearLayout for buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="20dp">

        <Button
            android:id="@+id/stop_download_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:background="@drawable/custom_button_background"
            android:textColor="@drawable/custom_button_text_color"
            style="@style/Widget.AppCompat.Button"
            android:visibility="gone"
            android:textAlignment="textStart"
            android:drawableStart="@android:drawable/ic_delete"
            android:text="@string/stop"/>

        <Button
            android:id="@+id/detail_play_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:background="@drawable/custom_button_background"
            android:textColor="@drawable/custom_button_text_color"
            style="@style/Widget.AppCompat.Button"
            android:visibility="gone"
            android:text="@string/play"
            android:textAlignment="textStart"
            android:drawableStart="@android:drawable/ic_media_play"
            android:drawablePadding="8dp"/>

        <Button
            android:id="@+id/detail_download_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_button_background"
            android:textColor="@drawable/custom_button_text_color"
            android:drawableStart="@android:drawable/ic_menu_save"
            android:textAlignment="textStart"
            android:visibility="gone"
            style="@style/Widget.AppCompat.Button"
            android:text="@string/save" />
    </LinearLayout>

    <!-- LOGGING AREA (MOVED TO BOTTOM) -->
    <TextView
        android:id="@+id/log_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textColor="#FF6B6B"
        android:textSize="10sp"
        tools:text="[ERROR] Not enough storage.[INFO] Download cancelled."/>

</LinearLayout>